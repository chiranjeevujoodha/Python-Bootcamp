select * from items order by price;

select * from items where price >=80 order by price desc;

select f_name, l_name from customers order by f_name limit 3;

select l_name from customers order by l_name desc;